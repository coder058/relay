# SOURCE: direct dependency versions in the environment passing the September audit.
# Transitive resolution is still checked by CI; this is not a full lockfile.
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
aiosqlite==0.22.1
httpx==0.28.1
pytest==9.1.1
pytest-asyncio==1.4.0
# SOURCE: official Python SDK stable version available on PyPI, verified 2026-08-28.
mcp==2.1.1
